# Server
PORT=3000
NODE_ENV=production

# Session (generate: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
SESSION_SECRET=your_long_random_secret_here

# Optional: forward contact submissions to Bateman AI
BATEMAN_API_URL=
FORWARD_CONTACTS_TO_BATEMAN=false
